import { IChildren } from "@/@types/children";
import { LayoutPage } from "@/components/LayoutPage";

const LayoutLogin = ({children}: IChildren) => <LayoutPage children={children} />

export default LayoutLogin;